$desktop = 1920px
$small-desktop = 1440px

$big-tablet = 1280px
$mid-tablet = 1024px
$small-tablet = 980px

$big-mobile = 768px
$semi-mobile = 600px
$mid-mobile = 480px
$small-mobile = 375px

mqSmallDesktop = "only screen and (max-width: " + $small-desktop + ")"

mqBigTablet = "only screen and (max-width: " + $big-tablet + ")"
mqMidTablet = "only screen and (max-width: " + $mid-tablet + ")"
mqSmallTablet = "only screen and (max-width: " + $small-tablet + ")"

mqBigMobile = "only screen and (max-width: " + $big-mobile + ")"
mqSemiMobile = "only screen and (max-width: " + $semi-mobile + ")"
mqMidMobile = "only screen and (max-width: " + $mid-mobile + ")"
mqSmallMobile = "only screen and (max-width: " + $small-mobile + ")"